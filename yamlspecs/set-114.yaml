## This is the CUDA11 version
!include packages.yaml
---
versions: versions-114.yaml
rversions: !include versions-114.yaml
bootstrap0:
   - cuda11-toolkit
   - cuda11-toolkit-static-libs
   - cuda-module
bootstrap:
   - "{{bootstrap0}}"
   - cudnn-download
build11:
   - cuda11-toolkit-nsight
   - cuda11-samples
build:
   - "{{build11}}" 
manifest:
   - "{{bootstrap0}}"
   - "{{build11}}"
