## This is the CUDA 11.4* version
!include packages.yaml
---
versions: versions-114.yaml
rversions: !include versions-114.yaml
build:
   - cuda-samples
manifest:
   - "{{bootstrap_cuda}}"
   - "{{build}}"
