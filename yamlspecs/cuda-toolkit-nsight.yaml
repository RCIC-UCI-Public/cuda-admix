!include cuda-toolkit.yaml
---
- package: CUDA toolkit nsight 
  name: "{{basename}}-nsight"
  description: >
    CUDA Toolkit Version {{version}} nsight Eclipse plugin 
  contents: nsightee_plugins libnsight NsightCompute-2019.3 NsightSystems-2019.3
  src_tarball: 
  build:
    configure: echo no configure required 
    pkgmake: echo files extracted during {{basename}} build 
    modules: None 
  files:
    - "{{root}}/*"
  requires: 
    - "{{basename}}_{{version}}"
