## This is the CUDA12 version
!include packages.yaml
---
versions: versions-122.yaml
rversions: !include versions-122.yaml
bootstrap0:
   - cuda12-toolkit
   - cuda12-toolkit-static-libs
   - cuda-module
bootstrap:
   - "{{bootstrap0}}"
   - cudnn-download
   - ffnvcodec
   - ffnvcodec-module
build:
   - "{{build12}}" 
build12:
   - cuda12-toolkit-nsight
   - cuda12-driver
   - ffmpeg60
   - ffmpeg60-module
manifest:
   - "{{bootstrap0}}"
   - "{{build12}}"
   - "{{cudnn.name}}-{{cudnn.version}}"
   - "{{cudnn.name}}-devel-{{cudnn.version}}"
   - "{{cudnn.name}}-samples-{{cudnn.version}}"
