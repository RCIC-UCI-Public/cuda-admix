## Build the 58 versions of libraries for CUDA
!include ffmpeg-common.yaml
---
- version: "{{versions.ffmpeg58}}"
  opt10: !OScmp ">=,10,--disable-inline-asm --disable-doc,"
  build:
     configure_args: --enable-shared {{opt10}} --disable-yasm --prefix={{root}}
