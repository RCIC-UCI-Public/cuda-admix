## This breaks out the cuda driver so that it can be updated more easily
##  
!include packages.yaml
---
versions: versions-cuda-driver.yaml
bootstrap:
   - cuda11-toolkit
build:
   - cuda11-driver
manifest:
   - "{{build}}"
