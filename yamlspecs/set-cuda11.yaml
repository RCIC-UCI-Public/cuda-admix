## This is the CUDA11 version
!include packages.yaml
---
versions: cuda11.versions.yaml
rversions: !include cuda11.versions.yaml
bootstrap0:
   - cuda11-toolkit
   - cuda11-toolkit-static-libs
   - cuda-module
bootstrap:
   - "{{bootstrap0}}"
   - cudnn-download
build11:
   - cuda11-toolkit-nsight
   - cuda11-samples
build:
   - "{{build_common}}"
   - "{{build11}}" 
   - cuda-driver
cudnn: 
  name: "{{rversions.cudnn.name}}" 
  version: "{{rversions.cudnn.version}}"
manifest:
   - "{{build11}}"
   - "{{bootstrap0}}"
   - "{{cudnn.name}}-{{cudnn.version}}"
   - "{{cudnn.name}}-devel-{{cudnn.version}}"
   - "{{cudnn.name}}-samples-{{cudnn.version}}"
