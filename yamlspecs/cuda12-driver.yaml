!include cuda12-common.yaml
!include cuda-driver-common.yaml
---
  src_tarball: "{{cuda.src_tarball}}"
  build:
    pkgmake: >
      if [ -d {{extractDir2}} ]; then /bin/rm -rf {{extractDir2}}; fi;
      mkdir {{extractDir2}};
      sh {{src_tarball}} --extract={{extractDir2}}
